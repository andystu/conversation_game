  <div class="reveal">

    <!-- Any section element inside of this container is displayed as a slide -->
    <div class="slides">
      <% for card in @conversation.cards %>
        <section>
          <h1>
            <%= card.content %>
          </h1>
        </section>
      <% end %>
    </div>
  </div>
